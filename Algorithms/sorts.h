#define TRUE 1
#define FALSE 0
void inline unsintswap(int *x, int *y);
// swaps two integer variables values
void inline unsintprintarr(int *arr, int size);
// prints out integer array
void inline unsintbubsort(int *arr, int size);
// performs bubble sort on integer array
void inline unsintselsort(int *arr, int size);
// performs selection sort on integer array
void inline unsintmersort(int *arr, int mid, int right);
// performs merge sort on integer array
void inline unsintinssort(int *arr, int size);
// performs insertion sort on integer array
void inline unsintinput();
// executes a text-based application with what you can give values to integer array and
// then choose sorting technique to apply to it.
// Uses recursion
void inline dblswap(double *x, double *y);
// swaps two double variables values
void inline dblprintarr(double *arr, int size);
// prints out double array
void inline dblbubsort(double arr[], int size);
// performs bubble sort on double array
void inline dblselsort(double arr[], int size);
// performs selection sort on double array
void inline dblmersort(double arr[], int mid, int right);
// performs merge sort on double array
void inline dblinssort(double arr[], int size);
// performs insertion sort on double array
void inline dblinput();
// executes a text-based application with what you can give values to double array and
// then choose sorting technique to apply to it.
// Uses recursion
void inline chswap(unsigned char *x, unsigned char *y);
// swaps two char variables values
void inline chprintarr(unsigned char *arr, int size);
// prints out char array
void inline chbubsort(unsigned char arr[], int size);
// performs bubble sort on char array
void inline chselsort(unsigned char arr[], int size);
// performs selection sort on char array
void inline chmersort(unsigned char arr[], int mid, int right);
// performs merge sort on char array
void inline chinssort(unsigned char arr[], int size);
// performs insertion sort on char array
void inline charinput();
// executes a text-based application with what you can give values to char array and
// then choose sorting technique to apply to it.
// Uses recursion
void inline print_char_in_dec_oct_hex(unsigned char arr[], int size);
// prints out char array in decimal code, octal code and hexadecimal code
void inline char_in_dec_oct_hex();
// executes a text-based application with what you can give values to char array and
// then see that array in decimal, octal and hexadecimal code.
// Uses recursion